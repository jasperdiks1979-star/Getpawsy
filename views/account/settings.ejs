<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
  <title>Account Settings | GetPawsy</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
  <script src="/js/pawsy.js" defer></script>
  <style>
    <%- include('../partials/account_styles') %>
    .pawsy-alert {
      padding: 1rem 1.5rem;
      border-radius: var(--pawsy-radius);
      margin-bottom: 1.5rem;
    }
    .pawsy-alert-success {
      background: #dcfce7;
      color: #166534;
      border: 1px solid #86efac;
    }
    .pawsy-alert-error {
      background: #fee2e2;
      color: #991b1b;
      border: 1px solid #fca5a5;
    }
  </style>
</head>
<body class="pawsy-body">
  <%- include("../partials/header") %>
  
  <main class="pawsy-main">
    <div class="pawsy-container">
      <div class="pawsy-account-page">
        <h1 class="pawsy-page-title">Account Settings</h1>

        <div class="pawsy-account-layout">
          <%- include('../partials/account_sidebar', { activePage: 'settings' }) %>

          <div class="pawsy-account-content">
            <% if (typeof success !== 'undefined' && success) { %>
              <div class="pawsy-alert pawsy-alert-success">
                ✓ <%= success %>
              </div>
            <% } %>
            <% if (typeof error !== 'undefined' && error) { %>
              <div class="pawsy-alert pawsy-alert-error">
                ✗ <%= error %>
              </div>
            <% } %>

            <section class="pawsy-account-section">
              <h2>Profile Information ⚙️</h2>
              
              <form action="/account/settings/update" method="POST" class="pawsy-settings-form">
                <div class="pawsy-form-group">
                  <label for="name">Full Name</label>
                  <input type="text" id="name" name="name" value="<%= user && user.name ? user.name : '' %>" class="pawsy-input" required>
                </div>
                <div class="pawsy-form-group">
                  <label for="email">Email Address</label>
                  <input type="email" id="email" name="email" value="<%= user && user.email ? user.email : '' %>" class="pawsy-input" readonly>
                  <small>Email cannot be changed for security reasons</small>
                </div>
                <div class="pawsy-form-group">
                  <label for="phone">Phone Number</label>
                  <input type="tel" id="phone" name="phone" value="<%= user && user.phone ? user.phone : '' %>" class="pawsy-input" placeholder="(555) 123-4567">
                </div>
                <button type="submit" class="pawsy-btn pawsy-btn-primary">Save Changes</button>
              </form>
            </section>

            <section class="pawsy-account-section">
              <h2>Change Password 🔐</h2>
              
              <form action="/account/settings/update" method="POST" class="pawsy-settings-form">
                <div class="pawsy-form-group">
                  <label for="currentPassword">Current Password</label>
                  <input type="password" id="currentPassword" name="currentPassword" class="pawsy-input">
                </div>
                <div class="pawsy-form-group">
                  <label for="newPassword">New Password</label>
                  <input type="password" id="newPassword" name="newPassword" class="pawsy-input" minlength="6">
                  <small>Minimum 6 characters</small>
                </div>
                <div class="pawsy-form-group">
                  <label for="confirmPassword">Confirm New Password</label>
                  <input type="password" id="confirmPassword" name="confirmPassword" class="pawsy-input">
                </div>
                <button type="submit" class="pawsy-btn pawsy-btn-primary">Update Password</button>
              </form>
            </section>

            <section class="pawsy-account-section">
              <h2>Email Preferences 📧</h2>
              
              <form class="pawsy-settings-form">
                <div class="pawsy-checkbox-group">
                  <label class="pawsy-checkbox">
                    <input type="checkbox" name="marketing" checked>
                    <span>Marketing emails and promotions</span>
                  </label>
                  <label class="pawsy-checkbox">
                    <input type="checkbox" name="orders" checked>
                    <span>Order updates and shipping notifications</span>
                  </label>
                  <label class="pawsy-checkbox">
                    <input type="checkbox" name="wishlist" checked>
                    <span>Wishlist price drop alerts</span>
                  </label>
                  <label class="pawsy-checkbox">
                    <input type="checkbox" name="rewards" checked>
                    <span>Loyalty rewards notifications</span>
                  </label>
                </div>
                <button type="button" class="pawsy-btn pawsy-btn-primary" onclick="alert('Email preferences saved!')">Save Preferences</button>
              </form>
            </section>

            <section class="pawsy-account-section pawsy-danger-zone">
              <h2>Danger Zone ⚠️</h2>
              <p>Once you delete your account, there is no going back. Please be certain.</p>
              <button type="button" class="pawsy-btn pawsy-btn-danger" onclick="if(confirm('Are you sure you want to delete your account? This action cannot be undone.')) alert('Please contact support to delete your account.')">Delete My Account</button>
            </section>
          </div>
        </div>
      </div>
    </div>
  </main>

  <%- include("../partials/footer") %>
  <%- include("../partials/pawsy_widget") %>
</body>
</html>
